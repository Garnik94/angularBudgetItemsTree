import { CalculateBudgetPipe } from './calculate-budget.pipe';

describe('CalculateBudgetPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateBudgetPipe();
    expect(pipe).toBeTruthy();
  });
});
