import { GetIdListsPipe } from './get-id-lists.pipe';

describe('GetIdListsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetIdListsPipe();
    expect(pipe).toBeTruthy();
  });
});
